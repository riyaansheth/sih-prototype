# Live AI paths (recommendation narrative + document extraction).
# Absent, the app falls back to the committed cache, then to a deterministic
# template — the dashboard works fully without a key.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5
